package xyz.auriium.mattlib2.hardware;

/**
 * A linear motor is a motor that contributes motion to a linear axis (not a closed set)
 * Check out the two constituent interfaces for more information
 */
public interface ILinearMotor extends ILinearEncoder, IActuator {
}
